spring:
  application:
    name: microservice-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xueyunlong123/springcloudstudy/    # 配置git仓库的地址
          search-paths: config-repo                             # git仓库地址下的相对地址，可以配置多个，用,分割。
          username:  xueyunlong123                              # git仓库的账号
          password:  1995xueyunlong                             # git仓库的密码
